# Thư viện lõi để chạy mô hình YOLO (Model detection & segmentation)
# Pin version đã kiểm thử để tránh lệch schema tracker config
ultralytics==8.4.27

# Thư viện xử lý ảnh và video (OpenCV)
opencv-python>=4.10.0

# Thư viện toán học xử lý mảng (dùng để xử lý tọa độ box)
numpy>=1.24.0

# Thư viện hỗ trợ thuật toán Tracking (Linear Assignment Problem)
lapx>=0.5.5

# Firebase Admin SDK (FCM push notifications + Firestore)
firebase-admin>=6.4.0